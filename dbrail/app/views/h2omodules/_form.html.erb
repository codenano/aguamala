<%= form_for(@h2omodule) do |f| %>
  <% if @h2omodule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@h2omodule.errors.count, "error") %> prohibited this h2omodule from being saved:</h2>

      <ul>
      <% @h2omodule.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
